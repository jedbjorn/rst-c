name: build

# Push/PR-time CI. Delegates to the reusable _build.yml so release.yml
# can share the exact same matrix without drift.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/_build.yml
